-- must use riverpod & avoid setstate
-- must code keep clean as much as possible
-- max code in one dart file 150 lines but if is it make serious issue then keep it
-- must use part/ or widgt/ as shown in home or authentication